# app.py reads PORT, not FLASK_PORT
PORT=8000
MONGODB_URI=
GOOGLE_API_KEY=
# Pin an explicit model. The -latest aliases drift to newer models with
# tighter free-tier quotas.
LLM_MODEL_NAME=models/gemini-2.5-flash
SERPAPI_KEY=
